import { FilterNameDestinationPipe } from './filter-name-destination.pipe';

describe('FilterNameDestinationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameDestinationPipe();
    expect(pipe).toBeTruthy();
  });
});
